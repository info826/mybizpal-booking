name: Booking Hourly Reminders
on:
  workflow_dispatch:

jobs:
  send:
    runs-on: ubuntu-latest

    steps:
    - name: Call reminder endpoint
      run: |
        curl -X POST "https://mybizpal-booking.onrender.com/cron/reminders" \
        -H "x-webhook-secret: ${{ secrets.WEBHOOK_SECRET }}"
